import 'package:flutter/material.dart';

class scerrenHome extends StatelessWidget {
  const scerrenHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this home page'),
    );
  }
}
